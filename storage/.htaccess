# Deny ALL direct access to storage — EPUBs, logs, cache, uploads.
# Downloads must go through api/download.php for auth + rate limiting + audit.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
